<form method="post" action="{{ path('app_releve_delete', {'id': releve.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ releve.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
